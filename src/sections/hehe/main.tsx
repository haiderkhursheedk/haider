"use client";

import Landing from "./landing";
import Navbar from "@/sections/global/Navbar";

export default function Main() {
    return (
      <section>
        <Navbar/>
        <Landing/>
      </section>
    );
}